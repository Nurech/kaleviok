{
  "$schema": "http://json-schema.org/schema",
  "$id": "GenerateStoreSchema",
  "type": "object",
  "name": "name",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the store",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    }
  },
  "required": ["name"]
}
